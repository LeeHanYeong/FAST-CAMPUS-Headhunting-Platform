{% extends 'base_narrow_header.jinja2' %}

{% block header_title %}비밀번호 재설정{% endblock %}

{% block content %}
    <div class="container-fluid">
        <div class="row justify-content-center">
            <div class="login-card card col-md-8 col-lg-5">
                <h5 class="text-center">비밀번호를 잊으셨나요?</h5>
                <p class="text-center">아래에 이메일을 기입하고 재설정 버튼을 누르시면 비밀번호 재설정 메일을 보내드립니다.</p>
                <form method="post">
                    {{ csrf_input }}
                    <div class="form-group row">
                        <label for="id_email" class="col-sm-2 col-form-label">이메일</label>
                        <div class="col-sm-10">
                            {{ form.email }}
                            {% for error in form.email.errors %}
                                <small class="form-text text-danger">{{ error }}</small>
                            {% endfor %}
                        </div>
                    </div>
                    <button class="btn btn-primary btn-block" type="submit">비밀번호 재설정</button>
                </form>
            </div>
        </div>
    </div>
{% endblock %}